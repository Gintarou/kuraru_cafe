<% if @sweet_menu.errors.any? %>
  <p>The form contains <%= pluralize(@sweet_menu.errors.count, "error") %></p>
  <p><br></p>
  <ul>
    <% @sweet_menu.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
